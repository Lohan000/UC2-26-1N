const ask = require('readline-sync')

/*
while (0 = ) {
    let question = Number(ask.question("Me da um número"))
    let question1 = Number(ask.question("Me da outro um número"))  
    let result = question + question1
}
//---------------------------------------

let soma = 0
let numUser = null

while (numUser !== 0){
    numUser = Number(ask.question(`Digite outro numero: `))
    soma = soma + numUser
}

console.log(soma)
*/

