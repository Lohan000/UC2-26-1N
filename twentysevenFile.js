const ask = require('readline-sync')
let myespecial = []
function addAccount(){
    let username = ask.question("novo usuario: ")
    let password = ask.question("Senha:", {hideEchoBAck:true, mask:'*'})
}
function loginAccount(){
    let username = ask.question("Digite o usuario: ")
    let password = ask.question("Senha:")
    if 
}