/*
const ask = require('readline-sync')
function maior|(array){
    i = 0
    j = 1
    switch (array)
    case (num(i) > num[j]):
        for(i < num.length; 1++;)



}
num = [11, 15, 18, 14, 13]
let i = 0
let j = i + 1
let omaior = []
function maior(num){
    if (num[i] >= num[j]){
    j = j + 1
    let = pop(omaior)
    omaior = [num[i]]
    
} else
    num[i] <= num[j]
    i = i + 1
    let = pop(omaior)
    omaior = [num[j]]
}
maior(num)
while(!(num[i] > num[j])){



}
*/
const myArray = [3, 6, 2, 9, 3, 10, 83, 9, 2, 0]

let maior = myArray[0]

for (let i = 0; i < myArray.length; i++){
    if (myArray[i] > maior){
        maior = myArray[i]
    }
}

console.log(maior)